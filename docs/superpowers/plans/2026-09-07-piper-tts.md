# Sprachausgabe über Piper — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Der Kurs spricht über einen eigenen Piper-Container statt über die Stimmen des Browsers — gleiche Stimme auf jedem Rechner, ohne Netzweg, mit vorladbaren Audiodateien.

**Architecture:** Ein interner `tts`-Dienst kann genau eine Sache: Text zu WAV. Das Backend ruft ihn auf, legt das Ergebnis in einem gedeckelten Dateispeicher ab und liefert es unter einer inhaltsbestimmten URL aus, die der Browser dauerhaft zwischenspeichern darf. Das Frontend bekommt drei Stufen: Server, Browserstimme, Text.

**Tech Stack:** Python 3.11 / FastAPI / `piper-tts` / httpx im Backend; React 18 / TypeScript / Vitest im Frontend; Docker Compose; Playwright für e2e.

**Spec:** `docs/superpowers/specs/2026-09-07-speaker-piper-tts-design.md`

## Global Constraints

- Alle Nutzertexte und Fehlermeldungen sind **auf Deutsch**.
- Der `tts`-Dienst hat **keinen `ports`-Eintrag** — er ist nur im Docker-Netz erreichbar.
- Der Audio-Endpunkt nimmt höchstens **300 Zeichen**; darüber `400`.
- Ein nicht erreichbarer `tts`-Dienst ergibt **`503`**, niemals `500`.
- Zeitgrenzen: **10 s** für die Synthese, **2 s** für `/api/audio/health`.
- Zwischenspeicher-Deckel: `AUDIO_CACHE_MAX_MB`, Vorgabe **50**; verdrängt wird **die am längsten nicht benutzte** Datei, und jeder Treffer frischt den Zeitstempel auf.
- Stimme: `PIPER_VOICE`, Vorgabe **`ru_RU-dmitri-medium`**.
- **Kein Modell in Tests laden.** `PiperVoice` wird überall durch eine Attrappe ersetzt.
- Der bestehende Browser-Tonweg bleibt vollständig erhalten und wird Stufe 2.
- Für eigene Prüfläufe `make test` und `make test-e2e` (headless) benutzen, **nie** `make test-e2e-show`.
- `make test` führt `npx tsc --noEmit` mit aus — Vitest prüft keine Typen.

## Dateiübersicht

**Neu**

| Datei | Verantwortung |
|---|---|
| `tts/Dockerfile` | Abbild mit `piper-tts` und dem beim Bauen geladenen Stimmmodell |
| `tts/requirements.txt` | `fastapi`, `uvicorn`, `piper-tts`, `pytest` |
| `tts/app.py` | `POST /synthesize`, `GET /health` — sonst nichts |
| `tts/tests/test_app.py` | Tests des Dienstes mit einer Piper-Attrappe |
| `backend/app/tts_client.py` | httpx-Anbindung an den Dienst, nach dem Muster von `ollama_client.py` |
| `backend/app/audio/cache.py` | Schlüssel, Ablage, Deckel, Verdrängung |
| `backend/tests/test_tts_client.py` | Zeitgrenzen und Fehlerfälle |
| `backend/tests/test_audio_cache.py` | Schlüsselbildung, Treffer, Verdrängung |
| `backend/tests/test_audio_routes.py` | Endpunkt: Treffer, `400`, `503`, Kopfzeilen |
| `frontend/src/audio/serverSpeech.ts` | URL bauen, vorladen, abspielen |
| `frontend/src/audio/serverSpeech.test.ts` | dieselben |
| `frontend/e2e/hoeren-server.spec.ts` | e2e mit abgefangenem Audio-Endpunkt |

**Geändert**

| Datei | Änderung |
|---|---|
| `docker-compose.yml` | Dienst `tts`, `TTS_HOST` und `AUDIO_CACHE_MAX_MB` beim Backend |
| `backend/app/config.py` | `tts_host`, `audio_cache_dir`, `audio_cache_max_mb` |
| `backend/app/dependencies.py` | `get_tts()` |
| `backend/app/api/routes.py` | `GET /api/audio`, `GET /api/audio/health` |
| `backend/requirements.txt` | unverändert — `httpx` ist schon da (hier nur zur Kontrolle vermerkt) |
| `frontend/src/audio/SpeechContext.tsx` | Vermittler zwischen Server- und Browserstimme |
| `frontend/src/ProfileView.tsx` | nennt auch die Serverstimme |
| `frontend/e2e/fixtures.ts` | fängt `/api/audio` standardmäßig ab |
| `scripts/smoke_test.sh` | prüft den Tonendpunkt |
| `Makefile` | `make ps`/`make logs` erfassen den neuen Dienst automatisch; nichts zu tun (Kontrollvermerk) |
| `README.md` | Dienstetabelle und Erklärung |

**Unverändert:** `frontend/src/audio/speech.ts` und `SpeakerButton.tsx` — die Browserstimme bleibt genau wie sie ist und wird Stufe 2.

---

### Task 1: Der `tts`-Dienst

**Files:**
- Create: `tts/app.py`, `tts/requirements.txt`, `tts/Dockerfile`, `tts/tests/test_app.py`, `tts/tests/__init__.py`

**Interfaces:**
- Produces:
  - `POST /synthesize` mit `{"text": str}` → `audio/wav`; leerer Text → `400`
  - `GET /health` → `{"status": "ok", "voice": "<PIPER_VOICE>"}`
  - `tts.app.synthesize_wav(text: str) -> bytes` — die Stelle, die Tests ersetzen

- [ ] **Step 1: Write the failing test**

`tts/tests/test_app.py`:

```python
import io
import wave

import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def client(monkeypatch):
    monkeypatch.setenv("PIPER_VOICE", "ru_RU-dmitri-medium")
    import tts.app as module

    def fake_synthesize(text: str) -> bytes:
        buffer = io.BytesIO()
        with wave.open(buffer, "wb") as wav:
            wav.setnchannels(1)
            wav.setsampwidth(2)
            wav.setframerate(22050)
            wav.writeframes(b"\x00\x00" * len(text))
        return buffer.getvalue()

    monkeypatch.setattr(module, "synthesize_wav", fake_synthesize)
    return TestClient(module.app)


def test_health_nennt_die_stimme(client):
    body = client.get("/health").json()
    assert body["status"] == "ok"
    assert body["voice"] == "ru_RU-dmitri-medium"


def test_synthesize_liefert_wav(client):
    response = client.post("/synthesize", json={"text": "дом"})
    assert response.status_code == 200
    assert response.headers["content-type"] == "audio/wav"
    assert response.content.startswith(b"RIFF")


def test_leerer_text_wird_abgelehnt(client):
    assert client.post("/synthesize", json={"text": "   "}).status_code == 400
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd tts && python -m pytest tests -q`
Expected: FAIL — `tts.app` existiert nicht.

- [ ] **Step 3: Write the implementation**

`tts/requirements.txt`:

```
fastapi==0.115.0
uvicorn[standard]==0.30.6
piper-tts==1.3.0
pytest==8.3.3
```

`tts/app.py`:

```python
"""Ein Dienst, eine Aufgabe: russischen Text zu WAV.

Bewusst ohne Zwischenspeicher — den führt das Backend, weil dort schon ein
Volume liegt und die Auslieferung ohnehin über das Backend läuft.
"""

import io
import os
import wave

from fastapi import FastAPI, HTTPException, Response
from pydantic import BaseModel

VOICE = os.environ.get("PIPER_VOICE", "ru_RU-dmitri-medium")
MODEL_PATH = os.environ.get("PIPER_MODEL_PATH", f"/models/{VOICE}.onnx")
LENGTH_SCALE = float(os.environ.get("PIPER_LENGTH_SCALE", "1.0"))
VOLUME = float(os.environ.get("PIPER_VOLUME", "1.0"))
MAX_CHARS = 300

app = FastAPI(title="Speaker TTS")

_voice = None


def _load_voice():
    """Das ONNX-Modell einmal je Prozess laden — je Anfrage waere es zu teuer."""
    global _voice
    if _voice is None:
        from piper import PiperVoice

        _voice = PiperVoice.load(MODEL_PATH)
    return _voice


def synthesize_wav(text: str) -> bytes:
    from piper import SynthesisConfig

    voice = _load_voice()
    config = SynthesisConfig(volume=VOLUME, length_scale=LENGTH_SCALE)
    buffer = io.BytesIO()
    with wave.open(buffer, "wb") as wav_file:
        voice.synthesize_wav(text, wav_file, syn_config=config)
    return buffer.getvalue()


class SynthesizeRequest(BaseModel):
    text: str


@app.get("/health")
def health() -> dict:
    return {"status": "ok", "voice": VOICE}


@app.post("/synthesize")
def synthesize(payload: SynthesizeRequest) -> Response:
    text = payload.text.strip()
    if not text:
        raise HTTPException(status_code=400, detail="Text ist leer")
    if len(text) > MAX_CHARS:
        raise HTTPException(status_code=400, detail=f"Text länger als {MAX_CHARS} Zeichen")
    return Response(content=synthesize_wav(text), media_type="audio/wav")
```

`tts/Dockerfile`:

```dockerfile
FROM python:3.11-slim

ARG PIPER_VOICE=ru_RU-dmitri-medium
ENV PIPER_VOICE=${PIPER_VOICE}
ENV PIPER_MODEL_PATH=/models/${PIPER_VOICE}.onnx

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Modell beim Bauen holen: kein Erstlauf-Download, laeuft danach ohne Internet.
RUN mkdir -p /models && python3 -m piper.download_voices ${PIPER_VOICE} --data-dir /models

COPY app.py .

EXPOSE 5001

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "5001"]
```

**Falls `--data-dir` von dieser piper-Version nicht unterstützt wird:** `python3 -m piper.download_voices --help` im Container ausführen und den passenden Schalter einsetzen; notfalls in `/models` wechseln und den Befehl dort ohne Schalter laufen lassen. Der Pfad muss am Ende zu `PIPER_MODEL_PATH` passen.

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd tts && python -m pytest tests -q`
Expected: PASS. Die Tests brauchen kein Modell — `synthesize_wav` ist ersetzt.

- [ ] **Step 5: Verify the image really builds and speaks**

```bash
docker build -t speaker-tts ./tts
docker run --rm -p 5001:5001 -d --name tts-probe speaker-tts
sleep 20
curl -sf localhost:5001/health
curl -sf -X POST localhost:5001/synthesize \
  -H 'content-type: application/json' \
  -d '{"text":"я говорю по-русски"}' -o /tmp/probe.wav
ls -l /tmp/probe.wav   # muss deutlich groesser als 44 Byte sein
docker rm -f tts-probe
```

Dieser Schritt ist **nicht optional**: die Tests sagen nichts darüber, ob Piper im Abbild überhaupt läuft.

- [ ] **Step 6: Commit**

```bash
git add tts/
git commit -m "feat(tts): add a Piper container that turns Russian text into WAV"
```

---

### Task 2: Anbindung des Dienstes im Backend

**Files:**
- Create: `backend/app/tts_client.py`, `backend/tests/test_tts_client.py`
- Modify: `backend/app/config.py`, `backend/app/dependencies.py`

**Interfaces:**
- Produces:
  - `TtsClient(host: str, timeout: float = 10.0, health_timeout: float = 2.0)`
  - `.synthesize(text: str) -> bytes` — wirft `TtsUnavailable` bei Verbindungsfehler oder Zeitüberschreitung
  - `.healthy() -> bool`
  - `TtsUnavailable(Exception)`
  - `settings.tts_host`, `settings.audio_cache_dir`, `settings.audio_cache_max_mb`
  - `dependencies.get_tts() -> TtsClient`

- [ ] **Step 1: Write the failing test**

`backend/tests/test_tts_client.py`:

```python
import httpx
import pytest

from app.tts_client import TtsClient, TtsUnavailable


def _client(handler, **kwargs) -> TtsClient:
    client = TtsClient(host="http://tts:5001", **kwargs)
    client._transport = httpx.MockTransport(handler)
    return client


def test_synthesize_reicht_die_wav_bytes_durch():
    def handler(request: httpx.Request) -> httpx.Response:
        assert request.url.path == "/synthesize"
        assert request.read() == b'{"text":"\\u0434\\u043e\\u043c"}'.replace(b"\\\\", b"\\")
        return httpx.Response(200, content=b"RIFFfake")

    assert _client(handler).synthesize("дом") == b"RIFFfake"


def test_verbindungsfehler_wird_zu_tts_unavailable():
    def handler(request: httpx.Request) -> httpx.Response:
        raise httpx.ConnectError("kein Dienst", request=request)

    with pytest.raises(TtsUnavailable):
        _client(handler).synthesize("дом")


def test_zeitueberschreitung_wird_zu_tts_unavailable():
    def handler(request: httpx.Request) -> httpx.Response:
        raise httpx.ReadTimeout("zu langsam", request=request)

    with pytest.raises(TtsUnavailable):
        _client(handler).synthesize("дом")


def test_fehlerstatus_wird_zu_tts_unavailable():
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(500, content=b"kaputt")

    with pytest.raises(TtsUnavailable):
        _client(handler).synthesize("дом")


def test_healthy_meldet_true_bei_ok():
    def handler(request: httpx.Request) -> httpx.Response:
        assert request.url.path == "/health"
        return httpx.Response(200, json={"status": "ok", "voice": "ru_RU-dmitri-medium"})

    assert _client(handler).healthy() is True


def test_healthy_meldet_false_statt_zu_werfen():
    def handler(request: httpx.Request) -> httpx.Response:
        raise httpx.ConnectError("kein Dienst", request=request)

    assert _client(handler).healthy() is False
```

Die Zeile mit `request.read()` ist umständlich; wenn sie stört, stattdessen
`assert b"\\u0434" in request.read() or "дом" in request.read().decode()` benutzen — geprüft werden
soll nur, dass der Text mitgeht.

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && .venv/bin/python -m pytest tests/test_tts_client.py -q -p no:cacheprovider`
Expected: FAIL — `app.tts_client` existiert nicht.

- [ ] **Step 3: Write the implementation**

`backend/app/tts_client.py`:

```python
import httpx


class TtsUnavailable(Exception):
    """Der Sprachdienst antwortet nicht — nie ein Serverfehler, immer ein 503."""


class TtsClient:
    def __init__(self, host: str, timeout: float = 10.0, health_timeout: float = 2.0):
        self._host = host.rstrip("/")
        self._timeout = timeout
        self._health_timeout = health_timeout
        self._transport: httpx.BaseTransport | None = None

    def _request(self, method: str, path: str, *, timeout: float, **kwargs) -> httpx.Response:
        with httpx.Client(transport=self._transport, timeout=timeout) as client:
            return client.request(method, f"{self._host}{path}", **kwargs)

    def synthesize(self, text: str) -> bytes:
        try:
            response = self._request(
                "POST", "/synthesize", timeout=self._timeout, json={"text": text}
            )
            response.raise_for_status()
        except httpx.HTTPError as exc:
            raise TtsUnavailable(str(exc)) from exc
        return response.content

    def healthy(self) -> bool:
        try:
            response = self._request("GET", "/health", timeout=self._health_timeout)
            return response.status_code == 200
        except httpx.HTTPError:
            return False
```

In `backend/app/config.py` das `Settings`-Dataclass erweitern:

```python
    tts_host: str = os.environ.get("TTS_HOST", "http://tts:5001")
    audio_cache_dir: str = os.environ.get("AUDIO_CACHE_DIR", "./data/audio")
    audio_cache_max_mb: int = int(os.environ.get("AUDIO_CACHE_MAX_MB", "50"))
```

In `backend/app/dependencies.py`:

```python
from app.tts_client import TtsClient


def get_tts() -> TtsClient:
    return TtsClient(host=settings.tts_host)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && .venv/bin/python -m pytest -q -p no:cacheprovider`
Expected: PASS, alle bestehenden Tests weiterhin grün.

- [ ] **Step 5: Commit**

```bash
git add backend/app/tts_client.py backend/app/config.py backend/app/dependencies.py backend/tests/test_tts_client.py
git commit -m "feat(backend): reach the speech service over httpx"
```

---

### Task 3: Zwischenspeicher mit Deckel

**Files:**
- Create: `backend/app/audio/__init__.py`, `backend/app/audio/cache.py`, `backend/tests/test_audio_cache.py`

**Interfaces:**
- Produces:
  - `audio_key(text: str, *, voice: str, length_scale: float) -> str` — 64 Hex-Zeichen
  - `strip_stress(text: str) -> str` — entfernt `U+0301`, Gegenstueck zu `stripStress` im Frontend
  - `AudioCache(directory: str | Path, max_mb: int)`
  - `.get(key: str) -> bytes | None` — frischt bei einem Treffer den Zeitstempel auf
  - `.put(key: str, data: bytes) -> None` — verdrängt danach, bis der Deckel eingehalten ist
  - `.total_bytes() -> int`

- [ ] **Step 1: Write the failing test**

`backend/tests/test_audio_cache.py`:

```python
import os
import time

from app.audio.cache import AudioCache, audio_key


def test_schluessel_ist_stabil_und_inhaltsbestimmt():
    a = audio_key("дом", voice="ru_RU-dmitri-medium", length_scale=1.0)
    b = audio_key("дом", voice="ru_RU-dmitri-medium", length_scale=1.0)
    assert a == b and len(a) == 64


def test_andere_stimme_ergibt_anderen_schluessel():
    a = audio_key("дом", voice="ru_RU-dmitri-medium", length_scale=1.0)
    b = audio_key("дом", voice="ru_RU-irina-medium", length_scale=1.0)
    assert a != b


def test_anderes_tempo_ergibt_anderen_schluessel():
    a = audio_key("дом", voice="ru_RU-dmitri-medium", length_scale=1.0)
    b = audio_key("дом", voice="ru_RU-dmitri-medium", length_scale=1.4)
    assert a != b


def test_ablegen_und_wiederholen(tmp_path):
    cache = AudioCache(tmp_path, max_mb=1)
    assert cache.get("abc") is None
    cache.put("abc", b"RIFFdaten")
    assert cache.get("abc") == b"RIFFdaten"


def test_deckel_verdraengt_bis_die_grenze_haelt(tmp_path):
    cache = AudioCache(tmp_path, max_mb=1)
    block = b"x" * 400_000
    for key in ("a", "b", "c"):
        cache.put(key, block)
        time.sleep(0.01)
    assert cache.total_bytes() <= 1_048_576
    assert cache.get("a") is None
    assert cache.get("c") == block


def test_verdraengt_das_am_laengsten_ungenutzte_nicht_das_aelteste(tmp_path):
    # Genau die Falle: die Saetze aus Einheit 1 sind die aeltesten, kommen in der
    # Wiederholung aber staendig dran.
    cache = AudioCache(tmp_path, max_mb=1)
    block = b"x" * 400_000
    cache.put("alt", block)
    time.sleep(0.01)
    cache.put("mittel", block)
    time.sleep(0.01)

    assert cache.get("alt") == block  # frischt den Zeitstempel auf
    time.sleep(0.01)

    cache.put("neu", block)
    assert cache.get("alt") == block, "die oft benutzte Datei darf nicht fliegen"
    assert cache.get("mittel") is None, "die laengste ungenutzte muss fliegen"


def test_treffer_frischt_den_zeitstempel_auf(tmp_path):
    cache = AudioCache(tmp_path, max_mb=1)
    cache.put("abc", b"daten")
    path = tmp_path / "abc.wav"
    os.utime(path, (1_000_000, 1_000_000))
    cache.get("abc")
    assert path.stat().st_mtime > 1_000_000
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && .venv/bin/python -m pytest tests/test_audio_cache.py -q -p no:cacheprovider`
Expected: FAIL — `app.audio.cache` existiert nicht.

- [ ] **Step 3: Write the implementation**

`backend/app/audio/__init__.py` bleibt leer. `backend/app/audio/cache.py`:

```python
import hashlib
import time
from pathlib import Path


def audio_key(text: str, *, voice: str, length_scale: float) -> str:
    """Inhaltsbestimmter Schluessel — deshalb darf die Antwort `immutable` heissen."""
    raw = f"{voice}|{length_scale}|{text}".encode("utf-8")
    return hashlib.sha256(raw).hexdigest()


class AudioCache:
    """Dateien auf der Platte mit hartem Deckel.

    Verdraengt wird nach Zeitstempel, und jeder Treffer frischt ihn auf. Ohne das
    waere es „aeltestes zuerst" statt „am laengsten nicht gebraucht" — und die
    Saetze aus Einheit 1 flogen als Erstes, obwohl sie am haeufigsten drankommen.
    """

    def __init__(self, directory: str | Path, max_mb: int):
        self._dir = Path(directory)
        self._dir.mkdir(parents=True, exist_ok=True)
        self._max_bytes = max_mb * 1024 * 1024

    def _path(self, key: str) -> Path:
        return self._dir / f"{key}.wav"

    def get(self, key: str) -> bytes | None:
        path = self._path(key)
        if not path.exists():
            return None
        now = time.time()
        path.touch()
        return path.read_bytes()

    def put(self, key: str, data: bytes) -> None:
        self._path(key).write_bytes(data)
        self._evict()

    def total_bytes(self) -> int:
        return sum(path.stat().st_size for path in self._dir.glob("*.wav"))

    def _evict(self) -> None:
        files = sorted(self._dir.glob("*.wav"), key=lambda path: path.stat().st_mtime)
        total = sum(path.stat().st_size for path in files)
        for path in files:
            if total <= self._max_bytes:
                return
            total -= path.stat().st_size
            path.unlink(missing_ok=True)
```

Die Zeile `now = time.time()` in `get` ist überflüssig — beim Schreiben weglassen.

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && .venv/bin/python -m pytest -q -p no:cacheprovider`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/app/audio backend/tests/test_audio_cache.py
git commit -m "feat(backend): cache synthesised audio under a hard size cap"
```

---

### Task 4: Der Audio-Endpunkt

**Files:**
- Modify: `backend/app/api/routes.py`, `backend/app/dependencies.py`
- Create: `backend/tests/test_audio_routes.py`

**Interfaces:**
- Consumes: `TtsClient`, `TtsUnavailable` (Task 2), `AudioCache`, `audio_key` (Task 3)
- Produces:
  - `GET /api/audio?text=<urlkodiert>` → `audio/wav` mit `Cache-Control: public, max-age=31536000, immutable`
  - `GET /api/audio/health` → `{"available": bool}`
  - `dependencies.get_audio_cache() -> AudioCache`

- [ ] **Step 1: Write the failing test**

`backend/tests/test_audio_routes.py`:

```python
import pytest
from fastapi.testclient import TestClient

from app.audio.cache import AudioCache
from app.dependencies import get_audio_cache, get_tts
from app.main import app
from app.tts_client import TtsUnavailable


class FakeTts:
    def __init__(self, *, healthy: bool = True, fail: bool = False):
        self._healthy = healthy
        self._fail = fail
        self.calls = 0

    def synthesize(self, text: str) -> bytes:
        self.calls += 1
        if self._fail:
            raise TtsUnavailable("kein Dienst")
        return b"RIFF" + text.encode("utf-8")

    def healthy(self) -> bool:
        return self._healthy


@pytest.fixture
def client(tmp_path):
    cache = AudioCache(tmp_path, max_mb=1)
    app.dependency_overrides[get_audio_cache] = lambda: cache
    yield TestClient(app)
    app.dependency_overrides.clear()


def _use(tts: FakeTts) -> None:
    app.dependency_overrides[get_tts] = lambda: tts


def test_liefert_wav_mit_dauerhaftem_cache_header(client):
    _use(FakeTts())
    response = client.get("/api/audio", params={"text": "дом"})
    assert response.status_code == 200
    assert response.headers["content-type"] == "audio/wav"
    assert "immutable" in response.headers["cache-control"]
    assert response.content.startswith(b"RIFF")


def test_zweiter_abruf_fragt_den_dienst_nicht_erneut(client):
    tts = FakeTts()
    _use(tts)
    client.get("/api/audio", params={"text": "дом"})
    client.get("/api/audio", params={"text": "дом"})
    assert tts.calls == 1


def test_zu_langer_text_wird_abgelehnt(client):
    _use(FakeTts())
    assert client.get("/api/audio", params={"text": "я" * 301}).status_code == 400


def test_leerer_text_wird_abgelehnt(client):
    _use(FakeTts())
    assert client.get("/api/audio", params={"text": "  "}).status_code == 400


def test_nicht_erreichbarer_dienst_ergibt_503(client):
    _use(FakeTts(fail=True))
    assert client.get("/api/audio", params={"text": "дом"}).status_code == 503


def test_health_meldet_verfuegbarkeit(client):
    _use(FakeTts(healthy=True))
    assert client.get("/api/audio/health").json() == {"available": True}
    _use(FakeTts(healthy=False))
    assert client.get("/api/audio/health").json() == {"available": False}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && .venv/bin/python -m pytest tests/test_audio_routes.py -q -p no:cacheprovider`
Expected: FAIL — `get_audio_cache` gibt es nicht, die Route auch nicht.

- [ ] **Step 3: Write the implementation**

In `backend/app/dependencies.py`:

```python
from functools import lru_cache

from app.audio.cache import AudioCache


@lru_cache(maxsize=1)
def _audio_cache() -> AudioCache:
    return AudioCache(settings.audio_cache_dir, max_mb=settings.audio_cache_max_mb)


def get_audio_cache() -> AudioCache:
    return _audio_cache()
```

In `backend/app/api/routes.py` — Importe ergänzen (`Response`, `Query` aus `fastapi`,
`audio_key`, `get_audio_cache`, `get_tts`, `TtsClient`, `TtsUnavailable`, `AudioCache`) und anhängen:

```python
AUDIO_MAX_CHARS = 300
AUDIO_CACHE_HEADER = "public, max-age=31536000, immutable"


@router.get("/audio/health")
def audio_health(tts: TtsClient = Depends(get_tts)) -> dict:
    return {"available": tts.healthy()}


@router.get("/audio")
def audio(
    text: str = Query(...),
    tts: TtsClient = Depends(get_tts),
    cache: AudioCache = Depends(get_audio_cache),
) -> Response:
    cleaned = text.strip()
    if not cleaned:
        raise HTTPException(status_code=400, detail="Text ist leer")
    if len(cleaned) > AUDIO_MAX_CHARS:
        raise HTTPException(
            status_code=400, detail=f"Text länger als {AUDIO_MAX_CHARS} Zeichen"
        )

    # Gemessen: derselbe Satz mit U+0301 ergibt bei Piper eine andere, laengere
    # Ausgabe — espeak-ng kennt die Zeichen fuer Russisch nicht und macht daraus
    # Artefakte. Also raus damit, bevor synthetisiert und geschluesselt wird.
    cleaned = strip_stress(cleaned)

    key = audio_key(
        cleaned,
        voice=settings.piper_voice,
        length_scale=settings.piper_length_scale,
    )
    data = cache.get(key)
    if data is None:
        try:
            data = tts.synthesize(cleaned)
        except TtsUnavailable as exc:
            raise HTTPException(
                status_code=503, detail="Sprachdienst nicht erreichbar"
            ) from exc
        cache.put(key, data)

    return Response(
        content=data,
        media_type="audio/wav",
        headers={"Cache-Control": AUDIO_CACHE_HEADER},
    )
```

**Wichtig:** `/audio/health` muss **vor** `/audio` stehen? Nein — die Pfade unterscheiden sich
eindeutig, FastAPI trennt sie korrekt. Beide Reihenfolgen funktionieren.

In `backend/app/config.py` zusätzlich:

```python
    piper_voice: str = os.environ.get("PIPER_VOICE", "ru_RU-dmitri-medium")
    piper_length_scale: float = float(os.environ.get("PIPER_LENGTH_SCALE", "1.0"))
```

Das Backend braucht diese beiden Werte, weil sie in den Zwischenspeicher-Schlüssel eingehen — sie
müssen mit denen des `tts`-Dienstes übereinstimmen und werden deshalb in `docker-compose.yml` an
beide Dienste gegeben (Task 5).

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && .venv/bin/python -m pytest -q -p no:cacheprovider`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/app backend/tests/test_audio_routes.py
git commit -m "feat(backend): serve synthesised audio from a content-addressed URL"
```

---

### Task 5: Der Dienst im Stack

**Files:**
- Modify: `docker-compose.yml`, `scripts/smoke_test.sh`, `README.md`

**Interfaces:**
- Consumes: das Abbild aus Task 1, den Endpunkt aus Task 4

- [ ] **Step 1: Add the service**

In `docker-compose.yml` einen Dienst ergänzen — **ohne `ports`**:

```yaml
  tts:
    build:
      context: ./tts
      args:
        PIPER_VOICE: ${PIPER_VOICE:-ru_RU-dmitri-medium}
    environment:
      - PIPER_VOICE=${PIPER_VOICE:-ru_RU-dmitri-medium}
      - PIPER_LENGTH_SCALE=${PIPER_LENGTH_SCALE:-1.0}
      - PIPER_VOLUME=${PIPER_VOLUME:-1.0}
```

und beim `backend` ergänzen:

```yaml
      - TTS_HOST=http://tts:5001
      - PIPER_VOICE=${PIPER_VOICE:-ru_RU-dmitri-medium}
      - PIPER_LENGTH_SCALE=${PIPER_LENGTH_SCALE:-1.0}
      - AUDIO_CACHE_DIR=/data/audio
      - AUDIO_CACHE_MAX_MB=${AUDIO_CACHE_MAX_MB:-50}
```

sowie `tts` in die `depends_on`-Liste des Backends.

`PIPER_VOICE` steht bewusst **zweimal**: der `tts`-Dienst braucht sie zum Sprechen, das Backend zum
Bilden des Zwischenspeicher-Schlüssels. Laufen sie auseinander, liefert der Zwischenspeicher Ton in
der falschen Stimme aus.

- [ ] **Step 2: Extend the smoke test**

In `scripts/smoke_test.sh` die Zählung auf 7 anheben und anhängen:

```bash
echo "6/7 Sprachdienst erreichbar"
curl -sf "$BASE/api/audio/health" | grep -q '"available":true'

echo "7/7 Ton wird erzeugt"
curl -sf "$BASE/api/audio?text=%D0%B4%D0%BE%D0%BC" -o /tmp/speaker-smoke.wav
test "$(stat -c%s /tmp/speaker-smoke.wav)" -gt 1000
rm -f /tmp/speaker-smoke.wav
```

- [ ] **Step 3: Bring the stack up and verify**

```bash
make deploy
make smoke
```

Expected: 7/7 grün. Der erste Aufruf dauert länger, weil Piper das Modell lädt.

- [ ] **Step 4: Update the README**

Die Dienstetabelle um eine Zeile erweitern:

| Dienst | Port | Zweck |
|---|---|---|
| tts | — | Piper-Sprachausgabe, nur intern erreichbar |

Dazu ein Absatz, dass die Stimme über `PIPER_VOICE` gewechselt wird, dass ein Wechsel den
Zwischenspeicher entwertet (die Stimme geht in den Schlüssel ein) und dass `AUDIO_CACHE_MAX_MB` den
Platzbedarf deckelt.

- [ ] **Step 5: Commit**

```bash
git add docker-compose.yml scripts/smoke_test.sh README.md
git commit -m "feat(stack): run the speech service alongside backend and frontend"
```

---

### Task 6: Serverton im Frontend

**Files:**
- Create: `frontend/src/audio/serverSpeech.ts`, `frontend/src/audio/serverSpeech.test.ts`

**Interfaces:**
- Produces:
  - `audioUrl(text: string): string` — `/api/audio?text=...` über die bestehende API-Basis
  - `prefetchAudio(text: string): Promise<void>` — wärmt den Browser-Zwischenspeicher, wirft nie
  - `playAudio(text: string, options?: { slow?: boolean }): Promise<void>` — wirft bei Fehlschlag
  - `serverAudioAvailable(): Promise<boolean>` — fragt `/api/audio/health`
  - `SLOW_PLAYBACK_RATE = 0.6`

- [ ] **Step 1: Write the failing test**

`frontend/src/audio/serverSpeech.test.ts`:

```ts
import { afterEach, describe, expect, it, vi } from "vitest";

import {
  SLOW_PLAYBACK_RATE,
  audioUrl,
  playAudio,
  prefetchAudio,
  serverAudioAvailable,
} from "./serverSpeech";

class FakeAudio {
  src = "";
  playbackRate = 1;
  preservesPitch = true;
  static lastInstance: FakeAudio | null = null;
  static shouldFail = false;
  constructor(src: string) {
    this.src = src;
    FakeAudio.lastInstance = this;
  }
  play(): Promise<void> {
    return FakeAudio.shouldFail ? Promise.reject(new Error("blockiert")) : Promise.resolve();
  }
}

afterEach(() => {
  vi.unstubAllGlobals();
  FakeAudio.shouldFail = false;
});

describe("audioUrl", () => {
  it("kodiert den Text", () => {
    expect(audioUrl("дом")).toContain("text=%D0%B4%D0%BE%D0%BC");
  });

  it("zeigt auf den Audio-Endpunkt", () => {
    expect(audioUrl("дом")).toContain("/api/audio?");
  });
});

describe("prefetchAudio", () => {
  it("holt die Datei vor", async () => {
    const fetchMock = vi.fn(() => Promise.resolve(new Response(new Blob())));
    vi.stubGlobal("fetch", fetchMock);
    await prefetchAudio("дом");
    expect(fetchMock).toHaveBeenCalledWith(audioUrl("дом"));
  });

  it("schluckt Fehler, statt den Aufrufer zu stören", async () => {
    vi.stubGlobal("fetch", vi.fn(() => Promise.reject(new Error("weg"))));
    await expect(prefetchAudio("дом")).resolves.toBeUndefined();
  });
});

describe("playAudio", () => {
  it("spielt die Datei ab", async () => {
    vi.stubGlobal("Audio", FakeAudio);
    await playAudio("дом");
    expect(FakeAudio.lastInstance?.src).toContain("text=");
    expect(FakeAudio.lastInstance?.playbackRate).toBe(1);
  });

  it("spielt langsam ohne die Stimme zu vertiefen", async () => {
    vi.stubGlobal("Audio", FakeAudio);
    await playAudio("дом", { slow: true });
    expect(FakeAudio.lastInstance?.playbackRate).toBe(SLOW_PLAYBACK_RATE);
    expect(FakeAudio.lastInstance?.preservesPitch).toBe(true);
  });

  it("wirft, wenn das Abspielen scheitert", async () => {
    vi.stubGlobal("Audio", FakeAudio);
    FakeAudio.shouldFail = true;
    await expect(playAudio("дом")).rejects.toThrow();
  });
});

describe("serverAudioAvailable", () => {
  it("meldet true, wenn der Dienst verfügbar ist", async () => {
    vi.stubGlobal("fetch", vi.fn(() => Promise.resolve(Response.json({ available: true }))));
    await expect(serverAudioAvailable()).resolves.toBe(true);
  });

  it("meldet false bei einem Netzfehler", async () => {
    vi.stubGlobal("fetch", vi.fn(() => Promise.reject(new Error("weg"))));
    await expect(serverAudioAvailable()).resolves.toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --prefix frontend -- serverSpeech`
Expected: FAIL — Modul existiert nicht.

- [ ] **Step 3: Write the implementation**

Erst nachsehen, wie `frontend/src/courseApi.ts` seine Basis-URL bildet (`import.meta.env.VITE_API_BASE_URL`
oder leer), und dieselbe Quelle benutzen — nicht eine zweite erfinden.

`frontend/src/audio/serverSpeech.ts`:

```ts
/**
 * Ton vom eigenen Server. Die URL ist inhaltsbestimmt und darf deshalb dauerhaft
 * im Browser liegen — genau das macht das Vorladen fast kostenlos.
 */

const BASE = import.meta.env.VITE_API_BASE_URL ?? "";

export const SLOW_PLAYBACK_RATE = 0.6;

export function audioUrl(text: string): string {
  return `${BASE}/api/audio?text=${encodeURIComponent(text)}`;
}

/** Waermt den Zwischenspeicher des Browsers. Fehler sind hier bedeutungslos. */
export async function prefetchAudio(text: string): Promise<void> {
  try {
    await fetch(audioUrl(text));
  } catch {
    // Vorladen ist Kuer, nicht Pflicht.
  }
}

export async function playAudio(text: string, options?: { slow?: boolean }): Promise<void> {
  const audio = new Audio(audioUrl(text));
  audio.preservesPitch = true;
  audio.playbackRate = options?.slow ? SLOW_PLAYBACK_RATE : 1;
  await audio.play();
}

export async function serverAudioAvailable(): Promise<boolean> {
  try {
    const response = await fetch(`${BASE}/api/audio/health`);
    if (!response.ok) return false;
    const body = (await response.json()) as { available?: boolean };
    return body.available === true;
  } catch {
    return false;
  }
}
```

`preservesPitch` ist in älteren TypeScript-DOM-Typen unbekannt; falls `tsc` meckert, in
`frontend/src/vite-env.d.ts` ergänzen:

```ts
interface HTMLMediaElement {
  preservesPitch: boolean;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test --prefix frontend -- serverSpeech` und `cd frontend && npx tsc --noEmit -p tsconfig.json`
Expected: PASS und keine Typfehler.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/audio/serverSpeech.ts frontend/src/audio/serverSpeech.test.ts
git commit -m "feat(audio): play speech from the server over a cacheable URL"
```

---

### Task 7: Die drei Stufen im `SpeechContext`

**Files:**
- Modify: `frontend/src/audio/SpeechContext.tsx`, `frontend/src/audio/SpeechContext.test.tsx`

**Interfaces:**
- Consumes: `serverSpeech.ts` (Task 6), `speech.ts` (unverändert)
- Produces: `useSpeech()` liefert zusätzlich `source: "server" | "browser" | "none"`; `available` heißt ab jetzt „Stufe 1 oder 2"; `activeVoice` nennt bei Serverton `{ name: "Piper", lang: "ru-RU", local: true }`

- [ ] **Step 1: Write the failing test**

An `frontend/src/audio/SpeechContext.test.tsx` anhängen:

```tsx
import * as server from "./serverSpeech";

describe("Drei Stufen", () => {
  function Stufe() {
    const { source, available, say } = useSpeech();
    return (
      <>
        <span data-testid="quelle">{source}</span>
        <span data-testid="ton">{String(available)}</span>
        <button type="button" onClick={() => say("дом")}>
          sprechen
        </button>
      </>
    );
  }

  it("nimmt den Server, wenn er antwortet", async () => {
    vi.spyOn(server, "serverAudioAvailable").mockResolvedValue(true);
    const play = vi.spyOn(server, "playAudio").mockResolvedValue();
    stubVoices([]);
    render(
      <SpeechProvider>
        <Stufe />
      </SpeechProvider>,
    );
    await waitFor(() => expect(screen.getByTestId("quelle")).toHaveTextContent("server"));
    screen.getByRole("button", { name: "sprechen" }).click();
    await waitFor(() => expect(play).toHaveBeenCalledWith("дом", { slow: false }));
  });

  it("nimmt die Browserstimme, wenn der Server schweigt", async () => {
    vi.spyOn(server, "serverAudioAvailable").mockResolvedValue(false);
    stubVoices(["ru-RU"]);
    render(
      <SpeechProvider>
        <Stufe />
      </SpeechProvider>,
    );
    await waitFor(() => expect(screen.getByTestId("quelle")).toHaveTextContent("browser"));
    screen.getByRole("button", { name: "sprechen" }).click();
    await waitFor(() => expect(spoken).toEqual(["дом"]));
  });

  it("meldet keinen Ton, wenn beides fehlt", async () => {
    vi.spyOn(server, "serverAudioAvailable").mockResolvedValue(false);
    stubVoices(["de-DE"]);
    render(
      <SpeechProvider>
        <Stufe />
      </SpeechProvider>,
    );
    await waitFor(() => expect(screen.getByTestId("quelle")).toHaveTextContent("none"));
    expect(screen.getByTestId("ton")).toHaveTextContent("false");
  });

  it("fällt auf den Browser zurück, wenn das Abspielen scheitert", async () => {
    vi.spyOn(server, "serverAudioAvailable").mockResolvedValue(true);
    vi.spyOn(server, "playAudio").mockRejectedValue(new Error("blockiert"));
    stubVoices(["ru-RU"]);
    render(
      <SpeechProvider>
        <Stufe />
      </SpeechProvider>,
    );
    await waitFor(() => expect(screen.getByTestId("quelle")).toHaveTextContent("server"));
    screen.getByRole("button", { name: "sprechen" }).click();
    await waitFor(() => expect(spoken).toEqual(["дом"]));
    await waitFor(() => expect(screen.getByTestId("quelle")).toHaveTextContent("browser"));
  });
});
```

`stubVoices` und `spoken` existieren bereits weiter oben in dieser Datei.

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --prefix frontend -- SpeechContext`
Expected: FAIL — `source` gibt es nicht.

- [ ] **Step 3: Write the implementation**

In `SpeechContext.tsx`:

- `source`-Zustand vom Typ `"server" | "browser" | "none"`, Anfangswert `"none"`.
- Ein `useEffect`, der `serverAudioAvailable()` aufruft; bei `true` wird `source` auf `"server"` gesetzt.
- Der bestehende Stimmen-Effekt setzt `source` nur dann auf `"browser"`, wenn nicht schon `"server"` gilt.
- `available` wird zu `source !== "none"`, bleibt aber `null`, solange beide Prüfungen laufen.
- `say` wird zu:

```tsx
  const say = useCallback(
    async (text: string, options?: { slow?: boolean }) => {
      setLastError((previous) => (previous === null ? previous : null));
      if (source === "server") {
        try {
          await playAudio(text, { slow: options?.slow ?? false });
          return;
        } catch {
          // Einmal als tot erkannt, nicht bei jedem Klick erneut probieren.
          setSource(voice ? "browser" : "none");
        }
      }
      if (!voice) return;
      speak(text, voice, options?.slow ? SLOW_RATE : NORMAL_RATE, setLastError);
    },
    [source, voice],
  );
```

- `activeVoice` liefert bei `source === "server"` `{ name: "Piper", lang: "ru-RU", local: true }`,
  sonst wie bisher die Browserstimme.

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm test --prefix frontend` und `cd frontend && npx tsc --noEmit -p tsconfig.json`
Expected: PASS. Bestehende Tests, die `useSpeech` mocken, brauchen zusätzlich `source: "browser"` —
das betrifft `ProfileView.test.tsx`, `ListenMeaningExercise.test.tsx`, `AudioPrompt.test.tsx`,
`ReviewView.test.tsx`, `exercises.test.tsx`, `SpeakerButton.test.tsx`, `AutoplayToggle.test.tsx`,
`UnitView.test.tsx`.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/audio/
git commit -m "feat(audio): fall back from server speech to the browser voice"
```

---

### Task 8: Vorladen beim Betreten einer Aufgabe

**Files:**
- Modify: `frontend/src/course/AudioPrompt.tsx`, `frontend/src/course/AudioPrompt.test.tsx`

**Interfaces:**
- Consumes: `prefetchAudio` (Task 6), `useSpeech().source` (Task 7)

- [ ] **Step 1: Write the failing test**

```tsx
it("holt den Ton beim Betreten vor, wenn er vom Server kommt", () => {
  const prefetch = vi.spyOn(server, "prefetchAudio").mockResolvedValue();
  vi.spyOn(context, "useSpeech").mockReturnValue({
    available: true,
    autoplay: false,
    setAutoplay: vi.fn(),
    say: vi.fn(),
    lastError: null,
    activeVoice: null,
    source: "server",
  });
  render(<AudioPrompt text="дом" promptDe="Haus" />);
  expect(prefetch).toHaveBeenCalledWith("дом");
});

it("lädt nichts vor, wenn der Ton aus dem Browser kommt", () => {
  const prefetch = vi.spyOn(server, "prefetchAudio").mockResolvedValue();
  vi.spyOn(context, "useSpeech").mockReturnValue({
    available: true,
    autoplay: false,
    setAutoplay: vi.fn(),
    say: vi.fn(),
    lastError: null,
    activeVoice: null,
    source: "browser",
  });
  render(<AudioPrompt text="дом" promptDe="Haus" />);
  expect(prefetch).not.toHaveBeenCalled();
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --prefix frontend -- AudioPrompt`
Expected: FAIL — es wird nichts vorgeladen.

- [ ] **Step 3: Write the implementation**

In `AudioPrompt.tsx` einen Effekt ergänzen:

```tsx
  useEffect(() => {
    if (source === "server") void prefetchAudio(text);
  }, [source, text]);
```

Der bestehende Autoplay-Effekt bleibt unverändert daneben stehen.

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm test --prefix frontend`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/course/
git commit -m "feat(course): prefetch the sentence when entering a listening exercise"
```

---

### Task 9: e2e mit abgefangenem Audio-Endpunkt

**Files:**
- Modify: `frontend/e2e/fixtures.ts`
- Create: `frontend/e2e/hoeren-server.spec.ts`

**Interfaces:**
- Consumes: das bestehende `stubSpeech` aus `fixtures.ts`

- [ ] **Step 1: Extend the fixture**

Der `tts`-Container läuft in e2e-Läufen nicht — ohne Abfangen liefe jeder Tonabruf ins Leere. In
`fixtures.ts` eine Hilfe ergänzen und im Standard-`page`-Fixture aufrufen:

```ts
/** Eine gueltige, sehr kurze WAV-Datei (44 Byte Kopf, keine Samples). */
const TINY_WAV = Buffer.from(
  "UklGRiQAAABXQVZFZm10IBAAAAABAAEAIlYAAESsAAACABAAZGF0YQAAAAA=",
  "base64",
);

export async function stubServerAudio(page: Page, available: boolean): Promise<void> {
  await page.route("**/api/audio/health", (route) =>
    route.fulfill({ json: { available } }),
  );
  await page.route("**/api/audio?*", (route) =>
    available
      ? route.fulfill({ body: TINY_WAV, contentType: "audio/wav" })
      : route.fulfill({ status: 503, json: { detail: "Sprachdienst nicht erreichbar" } }),
  );
}
```

Im `page`-Fixture standardmäßig `await stubServerAudio(page, false)` aufrufen, damit die bestehenden
Läufe unverändert die Browserstimme benutzen und nichts umgeschrieben werden muss.

- [ ] **Step 2: Write the failing test**

`frontend/e2e/hoeren-server.spec.ts`:

```ts
import { expect, test } from "@playwright/test";
import type { Page } from "@playwright/test";

import { stubServerAudio, stubSpeech } from "./fixtures";

async function goToAudioExercise(page: Page) {
  await page.goto("/kurs/8");
  await page.getByRole("button", { name: "Los geht's" }).click();
  for (let step = 0; step < 3; step += 1) {
    const check = page.getByRole("button", { name: "Prüfen" });
    if (await check.isVisible().catch(() => false)) {
      await page.locator("main button[aria-pressed]").first().click();
      await check.click();
    } else {
      const grid = page.locator("main .grid");
      if (await grid.isVisible().catch(() => false)) {
        const left = grid.locator("> div").first().locator("button");
        const right = grid.locator("> div").last().locator("button");
        const count = await left.count();
        for (let index = 0; index < count; index += 1) {
          await left.nth(index).click();
          await right.nth(index).click();
        }
      } else {
        await page.locator("main button[aria-pressed]").first().click();
      }
    }
    await page.getByRole("button", { name: "Weiter" }).click();
  }
}

test("holt den Ton vom Server, wenn der Dienst antwortet", async ({ page }) => {
  await stubServerAudio(page, true);
  await stubSpeech(page, ["de-DE"]);

  const abrufe: string[] = [];
  page.on("request", (request) => {
    if (request.url().includes("/api/audio?")) abrufe.push(request.url());
  });

  await goToAudioExercise(page);
  await expect(page.getByRole("button", { name: "Anhören", exact: true })).toBeVisible();
  expect(abrufe.length).toBeGreaterThan(0);
});

test("fällt bei 503 auf die Browserstimme zurück", async ({ page }) => {
  await stubServerAudio(page, false);
  await stubSpeech(page, ["ru-RU"]);

  await goToAudioExercise(page);
  await expect(page.getByRole("button", { name: "Anhören", exact: true })).toBeVisible();
  await page.getByRole("button", { name: "Anhören", exact: true }).click();

  const spoken = await page.evaluate(() => (window as any).__spoken as string[]);
  expect(spoken.length).toBeGreaterThan(0);
});
```

- [ ] **Step 3: Run the suite**

Run: `make test-e2e`
Expected: alle Läufe grün, auch die bestehenden.

- [ ] **Step 4: Commit**

```bash
git add frontend/e2e/
git commit -m "test(e2e): cover server speech and its fallback to the browser voice"
```

---

### Task 10: Abschließende Prüfung am laufenden Stack

**Files:** keine — reine Verifikation

- [ ] **Step 1: Run everything**

```bash
make test
make test-e2e
make validate
```

Expected: alles grün.

- [ ] **Step 2: Deploy and smoke**

```bash
make deploy
make smoke
```

Expected: 7/7 grün.

- [ ] **Step 3: Confirm real audio and caching**

```bash
# Erster Abruf erzeugt, zweiter kommt aus dem Zwischenspeicher.
time curl -sf "http://localhost:8000/api/audio?text=%D0%B4%D0%BE%D0%BC" -o /tmp/a.wav
time curl -sf "http://localhost:8000/api/audio?text=%D0%B4%D0%BE%D0%BC" -o /tmp/b.wav
cmp /tmp/a.wav /tmp/b.wav && echo "gleich"
docker compose exec backend ls -la /data/audio | head
```

Expected: der zweite Abruf ist deutlich schneller, die Dateien sind identisch, und im Verzeichnis
liegt genau eine `.wav`.

- [ ] **Step 4: Hand over for listening**

Den Nutzer bitten, `http://localhost:3000` neu zu laden, eine Hör-Aufgabe zu öffnen und zu sagen, ob
Klang, Lautstärke und Einsatzverzögerung passen. Im Profil steht dann „Aktive Stimme: Piper".

**Das ist die eigentliche Abnahme.** Alle Tests dieses Plans laufen gegen Attrappen; ob die Stimme
taugt, entscheidet sich am Ohr.

---

## Selbstprüfung des Plans

**Spec-Abdeckung**

| Spec-Abschnitt | Task |
|---|---|
| 3.1 `tts`-Dienst, Modell im Abbild, Umgebungsvariablen | 1 |
| 3.2 Endpunkt, 300 Zeichen, 503, httpx-Client, Zeitgrenzen | 2, 4 |
| 3.3 Zwischenspeicher, Schlüssel, Deckel, Zeitstempel auffrischen | 3 |
| 3 Architektur, `tts` ohne Port | 5 |
| 4 Drei Stufen, Vorladen, `playbackRate` statt zweiter Datei | 6, 7, 8 |
| 5 Bisherige Tonschicht bleibt, `available` umdefiniert, Profilanzeige | 7 |
| 6 Tests (tts, Backend, Frontend, Playwright, smoke) | 1, 2, 3, 4, 5, 6, 7, 9 |

**Abweichungen und Festlegungen, die die Spec offen ließ**

1. **`PIPER_VOICE` steht in `docker-compose.yml` zweimal** — beim `tts`-Dienst zum Sprechen, beim
   Backend zum Bilden des Schlüssels. Die Spec nennt die Variable, sagt aber nicht, dass beide
   Dienste sie brauchen. Laufen sie auseinander, liefert der Zwischenspeicher die falsche Stimme aus.
2. **Das e2e-Standardfixture fängt `/api/audio` mit `available: false` ab.** Damit bleiben alle
   bestehenden e2e-Läufe auf der Browserstimme und müssen nicht angefasst werden.
3. **`PIPER_MODEL_PATH`** kommt als vierte Umgebungsvariable dazu, weil der Ablageort des Modells im
   Abbild vom Namen der Stimme abhängt.

**Typkonsistenz geprüft:** `TtsUnavailable` (Task 2) → `503` (Task 4); `audio_key` mit
`voice`/`length_scale` (Task 3) → `settings.piper_voice`/`piper_length_scale` (Task 4);
`serverAudioAvailable`/`playAudio`/`prefetchAudio` (Task 6) → `SpeechContext` (Task 7) →
`AudioPrompt` (Task 8); `source: "server" | "browser" | "none"` durchgängig.
